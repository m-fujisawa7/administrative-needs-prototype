# AGENTS.md

このリポジトリで作業するAIエージェント向けの指示です。リポジトリ全体に適用します。

## 現在のスコープ

現在の成果物は、行政ニーズの収集対象を定義する「情報源台帳」、入口URLの疎通・候補抽出を手動確認する「情報源チェック機能」、指定した個別ページの本文抽出を手動確認する「個別ページ抽出チェック機能」、指定したPDFのテキスト抽出を手動確認する「PDF抽出チェック機能」、取得した1案件をClaude CLIまたはMockで判定する「AI判定・構造化チェック機能」、Notionデータベースとデータソース構成を読み取り確認する「Notion接続チェック機能」、選定済み1件をプレビュー後に明示的な`--write`で新規作成する「Notion1件登録チェック機能」、人が選定したURLファイルを最大20件まで直列処理する「Notion選定URLバッチ機能」です。

ユーザーから明示的に依頼されない限り、次の機能を追加しないでください。

- 候補URLの継続収集、複数ページの一括取得
- PDFのOCR、画像解析、複数PDFの一括取得
- 複数案件の一括AI処理、複数AI比較、自動フォールバック
- AI解析結果のファイル・データベース保存
- Notionへの複数件登録、既存ページの更新・削除、データベース・データソース・プロパティの作成や変更
- Notion以外の外部サービス連携
- データベース、定期実行、通知、管理画面

将来の収集処理を想定した値や型は定義できますが、取得処理を先回りして実装しないでください。

## 作業開始時に読むファイル

情報源台帳を変更する前に、必ず次の順で確認してください。

1. `README.md`
2. `config/README.md`
3. `config/sources.yaml`
4. 情報源チェックを変更する場合は `src/source-check/README.md`
5. 個別ページ抽出チェックを変更する場合は `src/content-check/README.md`
6. PDF抽出チェックを変更する場合は `src/pdf-check/README.md`
7. AI判定を変更する場合は `src/ai/README.md`、`prompts/ai-check.md`、`config/company-fit-criteria.yaml`
8. Notion接続チェックを変更する場合は `src/notion-check/README.md`
9. Notion1件登録チェックを変更する場合は `src/notion-register/README.md`
10. Notion選定URLバッチを変更する場合は `src/notion-batch/README.md`
11. `src/source-registry/schema.ts`
12. 関連する既存テスト

READMEとコードの記述が異なる場合は、実際のバリデーションを定義する `src/source-registry/schema.ts` を確認し、矛盾を勝手に解消せず報告してください。

## 台帳編集の原則

- URL、組織名、ページ名を推測で登録しない。
- 自治体・公共機関の公式サイトで確認できたURLだけを登録する。
- 編集前に組織ID、情報源ID、URLの重複を確認する。
- 既存IDは外部参照に使われる可能性があるため、明示的な依頼なしに変更しない。
- 既存項目を整形目的で並べ替えたり、一括修正したりしない。
- 1回の変更は依頼された組織・情報源に限定し、必要最小限の差分にする。
- CSSセレクターは実ページの構造を確認できた場合だけ設定する。
- 確認できない情報源を `verified` にしない。状況に応じて `unverified` または `needs_review` を使う。
- 不確かな既存項目は勝手に削除せず、問題点を報告する。
- 無効化する場合は `enabled: false` とし、理由を `notes` に残す。

## 情報源を追加するときの流れ

1. `config/sources.yaml` の既存登録を検索する。
2. 公式URLとページ名称を確認する。
3. `config/README.md` の許可値とテンプレートに従って追加する。
4. 既存組織の場合は既存の `organization_id` を使う。
5. 新しい組織の場合は、先に `organizations` へ追加する。
6. 検証と一覧表示を実行する。
7. 差分を確認し、依頼外の変更がないことを確認する。

## 必須の検証

台帳または台帳関連コードを変更した場合は、リポジトリルートで次を実行してください。

```bash
npm run sources:validate
npm run sources:list
npm run lint
npm run typecheck
npm test
```

上記の検証は外部サイトへアクセスしません。`npm run sources:check`、`npm run content:check`、`npm run pdf:check`、`npm run ai:check`、`npm run notion:check` は、ユーザーから実アクセスの依頼または許可がある場合だけ実行してください。実AI確認は外部送信と費用が発生し得るため、明示されたURLと件数だけを順番に実行してください。fixture・Mockテスト成功を実アクセス・実AI・実Notion接続成功として報告しないでください。

チェック結果を保存する必要がある場合だけ `--output` を指定してください。保存先を省略すると `data/logs/source-check/source-id.json` へ保存し、同じ対象の最新結果で上書きします。日付・時刻を含むファイル名で実行履歴を増やさないでください。保存結果から台帳を自動更新せず、内容を人が確認してから `verification_status` と `last_verified_at` を手動更新してください。`data/logs/` のファイルはGitへコミットしません。

個別ページ抽出結果は、必要な場合だけ `--output` で `data/logs/content-check/source-id.json` へ保存し、同じ情報源の最新結果で上書きしてください。HTMLと本文全文は保存せず、保存JSONをGitへコミットしません。抽出できた本文が行政ニーズとして適切かは自動判定したと報告せず、プレビューを人が確認します。

PDF抽出結果は、追加・変更時または障害の再確認で必要な場合だけ `--output` で `data/logs/pdf-check/source-id.json` へ保存し、同じ情報源の最新結果で上書きしてください。PDFファイル、ページ別本文、本文全文は保存せず、保存JSONをGitへコミットしません。OCRや意味的な行政ニーズ判定を実施したと報告せず、プレビューを人が確認します。

AI判定は自治体公式サイトの公開文書だけを入力し、秘密情報、非公開資料、認証後ページ、個人情報を追加して送信しないでください。`ai:check` は結果を保存しません。`config/company-fit-criteria.yaml` のA・B・C・対象外基準、判定、根拠引用、自社関連度、コンタクト推奨度は人が確認し、AI結果だけで外部連絡や意思決定を行わないでください。

Notion接続チェックは`src/notion-check/README.md`に従い、データベース取得とデータソース取得だけを行ってください。Notion1件登録チェックは`src/notion-register/README.md`、選定URLバッチは`src/notion-batch/README.md`に従い、デフォルトをプレビューとし、ユーザーが明示したURLと`--write`がある場合だけ新規ページを作成してください。Mock解析結果、スキーマ不一致、複数データソース、重複URL、既存にないselect・multi_select選択肢がある場合は作成しないでください。`.env`、`NOTION_TOKEN`、Authorizationヘッダーを表示・ログ・変更・コミットしないでください。既存ページの更新・削除、データベース、データソース、プロパティ・選択肢の作成や変更は行わないでください。

依存関係がまだない環境では、先に `npm ci` を実行してください。`package-lock.json` を意図なく更新しないでください。

## 作業完了時の報告

次を簡潔に報告してください。

- 追加・変更した組織と情報源
- 公式URLを確認した根拠と確認日
- `enabled`、`priority`、分類を選んだ理由
- 実行した検証コマンドと結果
- 確認できなかった事項や残る不確実性

URLへアクセスしていない場合は、アクセスしたように報告しないでください。

## 既存作業の保護

- 作業開始時に `git status` を確認する。
- ユーザーや別作業者の未コミット変更を上書き・削除しない。
- 依頼と関係のないファイルを変更しない。
- 破壊的なGit操作や履歴の書き換えを行わない。
